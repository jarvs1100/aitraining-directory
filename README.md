# AI Training Directory

Public static directory site for AI training and L&D tools.

## Structure
- `site/` generated static website
- `data/listings.json` listing source data
- `scripts/build.js` static generator
- `CNAME` custom domain

## Build
```bash
node scripts/build.js
```
