---
import Base from '../../layouts/Base.astro';
import { useCases, roles, industries } from '../../lib/programmatic.js';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
const canonical = 'https://aitraining.directory/solutions/';
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: 'AI training solutions hub',
  description: 'Use-case, role, and industry pages to shortlist AI training tools for L&D and enablement teams.',
  url: canonical
};
---
<Base title="AI Training Solutions by Use Case, Role, and Industry" description="Browse AI training software recommendations by use case, role, and industry context for L&D and enablement teams." canonical={canonical} jsonLd={jsonLd}>
  <Breadcrumbs items={[{ label: 'Home', href: '/' }, { label: 'Solutions', href: '/solutions/' }]} />

  <section class="hero" style="margin-bottom:1.25rem;">
    <span class="eyebrow">Implementation-first cluster</span>
    <h1 style="margin-top:.7rem;">AI training solutions hub</h1>
    <p class="muted" style="max-width:72ch;">This hub targets high-intent L&D buying and implementation journeys. Each route includes practical frameworks, rollout steps, and FAQs based on real training operations—not generic tool roundups.</p>
    <div class="hero-actions">
      <a class="btn btn-primary" href="/compare/">Compare alternatives</a>
      <a class="btn btn-secondary" href="/categories/">Browse categories</a>
      <a class="btn btn-secondary" href="/solutions/ai-training-needs-analysis/">Start with needs analysis</a>
    </div>
  </section>

  <section class="section card">
    <h2>Top-intent routes for L&D teams</h2>
    <div class="chips">
      <a class="chip" href="/solutions/sop-to-video-training/">SOP to Video Training</a>
      <a class="chip" href="/solutions/new-hire-onboarding-automation/">New Hire Onboarding Automation</a>
      <a class="chip" href="/solutions/compliance-training-content-creation/">Compliance Training Content</a>
      <a class="chip" href="/solutions/sales-enablement-training/">Sales Enablement Training</a>
      <a class="chip" href="/solutions/customer-support-training/">Customer Support Training</a>
      <a class="chip" href="/solutions/multilingual-employee-training/">Multilingual Employee Training</a>
      <a class="chip" href="/solutions/eu-ai-act-ai-literacy-training/">EU AI Act AI Literacy Training</a>
    </div>
  </section>

  <section aria-labelledby="use-cases-heading" class="section">
    <h2 id="use-cases-heading">Use cases</h2>
    <div class="grid">
      {useCases.map((p) => (
        <a class="card" href={`/solutions/${p.slug}/`}>
          <h3 style="margin:0 0 8px 0;">{p.label}</h3>
          <p style="margin:0;" class="muted">{p.meta}</p>
        </a>
      ))}
    </div>
  </section>

  <section aria-labelledby="roles-heading" class="section">
    <h2 id="roles-heading">Roles</h2>
    <div class="grid">
      {roles.map((p) => (
        <a class="card" href={`/solutions/${p.slug}/`}>
          <h3 style="margin:0 0 8px 0;">{p.label}</h3>
          <p style="margin:0;" class="muted">Recommendations by role and workflow maturity.</p>
        </a>
      ))}
    </div>
  </section>

  <section aria-labelledby="industries-heading" class="section">
    <h2 id="industries-heading">Industries</h2>
    <div class="grid">
      {industries.map((p) => (
        <a class="card" href={`/solutions/${p.slug}/`}>
          <h3 style="margin:0 0 8px 0;">{p.label}</h3>
          <p style="margin:0;" class="muted">Filter for compliance and operating constraints.</p>
        </a>
      ))}
    </div>
  </section>
</Base>
