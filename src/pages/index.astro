---
import Base from '../layouts/Base.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import { allListings, getCategories } from '../lib/data.js';
import { getSiteAlternates } from '../lib/i18n.js';

const categories = getCategories();
const featured = allListings.slice(0, 12);

const lastReviewedDate = '2026-02-17';

const alternates = getSiteAlternates();

const homepageFaq = [
  {
    q: 'How often is AI Training Directory reviewed?',
    a: 'Core pages are reviewed in active operating cycles with build verification before publish.'
  },
  {
    q: 'Is this a generic AI tools list?',
    a: 'No. The directory is structured around L&D workflows such as onboarding, SOP updates, compliance refreshes, and enablement rollouts.'
  }
];

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'CollectionPage',
      name: 'AI Training Directory',
      description: 'Curated AI tools for employee training and learning & development teams.',
      url: 'https://aitraining.directory/',
      dateModified: lastReviewedDate,
      hasPart: featured.map((tool, i) => ({
        '@type': 'ListItem',
        position: i + 1,
        url: `https://aitraining.directory/tool/${tool.slug}/`,
        name: tool.name
      }))
    },
    {
      '@type': 'FAQPage',
      mainEntity: homepageFaq.map((item) => ({
        '@type': 'Question',
        name: item.q,
        acceptedAnswer: {
          '@type': 'Answer',
          text: item.a
        }
      }))
    }
  ]
};
---
<Base
  title="AI Training Directory | Best AI Tools for L&D and Employee Training"
  description="Discover top AI tools for employee training, SOP-to-video workflows, onboarding, and corporate learning operations."
  canonical="https://aitraining.directory/"
  jsonLd={jsonLd}
  alternates={alternates}
>
  <Breadcrumbs items={[{ label: 'Home', href: '/' }]} />

  <section class="hero">
    <span class="eyebrow">Built for L&D teams</span>
    <h1 style="margin-top:.7rem;max-width:18ch;">Find AI tools that cut training production time without hurting quality</h1>
    <p class="muted" style="max-width:72ch;">Skip generic AI directories. This site is built for high-intent training workflows: SOP updates, onboarding systems, compliance refreshes, enablement rollouts, and multilingual delivery. Use it to move from research to pilot decisions with less noise.</p>
    <div class="hero-actions">
      <a class="btn btn-primary" href="/solutions/">Start with solution pages</a>
      <a class="btn btn-secondary" href="/compare/">Compare alternatives</a>
      <a class="btn btn-secondary" href="/categories/">Explore categories</a>
    </div>
  </section>

  <section class="section card" aria-labelledby="trust-proof-heading" style="margin-top:0;">
    <h2 id="trust-proof-heading">Why teams trust this directory</h2>
    <ul>
      <li><strong>Workflow-first curation:</strong> every route is structured around real L&D execution workflows, not generic feature dumps.</li>
      <li><strong>Decision-ready pages:</strong> solution and comparison content is built for pilot planning, stakeholder alignment, and rollout governance.</li>
      <li><strong>Freshness cadence:</strong> pages are reviewed in active operating cycles and updated as implementation patterns evolve.</li>
    </ul>
    <p class="muted" style="margin-top:.75rem;">Update cadence: this project runs daily operator passes with build verification before publish.</p>
  </section>

  <section class="section card" aria-labelledby="review-metadata-heading">
    <h2 id="review-metadata-heading">Content freshness</h2>
    <p class="muted" style="margin:0;">Last reviewed: <time datetime={lastReviewedDate}>{lastReviewedDate}</time> (UTC). Verification includes build pass and link/path checks before publish.</p>
  </section>

  <section class="section card" aria-labelledby="framework-heading">
    <h2 id="framework-heading">How to evaluate AI training tools in 30 days</h2>
    <ol>
      <li><strong>Week 1:</strong> choose one high-friction workflow and baseline your current cycle time.</li>
      <li><strong>Week 2:</strong> run a controlled pilot with one content owner and one reviewer.</li>
      <li><strong>Week 3:</strong> measure quality and business outcomes (completion, readiness, error reduction).</li>
      <li><strong>Week 4:</strong> document SOPs, approval gates, and rollout plan for scale.</li>
    </ol>
    <div class="chips chips-nav">
      <a class="chip" href="/solutions/ai-tools-for-training-and-development/">Best AI tools for training & development</a>
      <a class="chip" href="/solutions/sop-to-video-training/">SOP to video playbook</a>
      <a class="chip" href="/solutions/new-hire-onboarding-automation/">Onboarding automation</a>
      <a class="chip" href="/solutions/compliance-training-content-creation/">Compliance content workflows</a>
    </div>
  </section>

  <section class="section card" aria-labelledby="decision-paths-heading">
    <h2 id="decision-paths-heading">Decision paths from this homepage</h2>
    <p class="muted">Move from discovery to shortlist by jumping across solution routes, comparison pages, and implementation tools in one flow.</p>
    <div class="chips chips-nav">
      <a class="chip" href="/solutions/">Open all solution routes</a>
      <a class="chip" href="/compare/">Open all compare routes</a>
      <a class="chip" href="/tool/synthesia/">Review Synthesia implementation fit</a>
      <a class="chip" href="/compare/scorm-authoring-vs-lms-native-builders/">SCORM vs LMS-native builder framework</a>
      <a class="chip" href="/compare/ai-roleplay-simulators-vs-video-only-onboarding/">Roleplay vs video-only onboarding framework</a>
    </div>
  </section>

  <section class="section" aria-labelledby="category-heading">
    <h2 id="category-heading">Popular categories</h2>
    <div class="chips chips-nav">
      {categories.map((c) => <a class="chip" href={`/category/${c.slug}/`}>{c.name} <span aria-hidden="true">·</span> {c.count}</a>)}
    </div>
  </section>

  <section class="section" aria-labelledby="featured-heading">
    <h2 id="featured-heading">Featured tools</h2>
    <div class="grid">
      {featured.map((tool) => (
        <article class="card">
          <div class="chips" style="margin-bottom:.85rem;">
            <span class="badge">{tool.category}</span>
            <span class="badge">{tool.pricing}</span>
          </div>
          <h3 style="margin-bottom:.45rem;"><a href={`/tool/${tool.slug}/`} style="color:inherit;">{tool.name}</a></h3>
          <p class="muted" style="margin-bottom:1rem;">{tool.summary}</p>
          <a href={tool.website} rel="nofollow noopener" target="_blank">Visit website ↗</a>
        </article>
      ))}
    </div>
  </section>

  <section class="section" aria-labelledby="homepage-faq-heading">
    <h2 id="homepage-faq-heading">FAQ</h2>
    {homepageFaq.map((item) => (
      <>
        <h3>{item.q}</h3>
        <p>{item.a}</p>
      </>
    ))}
  </section>

  <section class="section card" aria-labelledby="distribution-playbook-heading">
    <h2 id="distribution-playbook-heading">Distribution micro-playbook</h2>
    <p class="muted">Need to turn this directory into traffic? Use the operating checklist for outreach channels, partner loops, and repeatable weekly distribution.</p>
    <a href="https://github.com/jarvs1100/aitraining-directory/blob/main/ops/outreach-distribution-checklist.md" target="_blank" rel="noopener">Open outreach distribution checklist ↗</a>
  </section>
</Base>
